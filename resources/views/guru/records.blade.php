@php($title='Catatan Poin') @php($navigation=[['guru.dashboard','Ringkasan'],['guru.records','Catatan poin']]+(auth()->user()->hasRole('Guru BK')?[['guru.students','Rekap siswa'],['guru.reports','Laporan kesiswaan'],['guru.letters','Surat panggilan']]:[]))
<x-layouts.app :title="$title" :navigation="$navigation"><x-dashboard title="Catatan poin" eyebrow="KEJADIAN SISWA" copy="Catat pelanggaran atau apresiasi dengan kronologi yang dapat ditindaklanjuti."/><section class="panel"><form method="POST" action="{{ route('guru.records.store') }}" enctype="multipart/form-data"><div class="form-grid">@csrf<label>Siswa<select name="siswa_id" required><option value="">Pilih siswa</option>@foreach($students as $student)<option value="{{ $student->id }}">{{ $student->user->nama_lengkap }} · {{ $student->nisn }}</option>@endforeach</select></label><label>Kategori<select name="kategori_poin_id" required><option value="">Pilih kategori</option>@foreach($categories as $category)<option value="{{ $category->id }}">{{ ucfirst($category->jenis) }} · {{ $category->nama_kategori }} ({{ $category->bobot_poin }})</option>@endforeach</select></label><label>Tanggal<input type="date" name="tanggal" value="{{ now()->format('Y-m-d') }}" required></label><label>Bukti foto<input type="file" name="bukti_foto" accept="image/*"></label></div><label>Kronologi<textarea name="keterangan" required placeholder="Jelaskan kejadian secara objektif"></textarea></label><button class="button primary" type="submit">Simpan catatan</button></form></section><section class="panel"><div class="panel-heading"><h3>Riwayat dan validasi</h3></div><div class="table-wrap"><table><thead><tr><th>Siswa</th><th>Kategori</th><th>Tanggal</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($records as $record)<tr><td>{{ $record->siswa->user->nama_lengkap }}</td><td>{{ $record->kategoriPoin->nama_kategori }}</td><td>{{ $record->tanggal->format('d/m/Y') }}</td><td>{{ $record->status_validasi }}</td><td>@if(auth()->user()->hasRole('Guru BK') && $record->status_validasi === 'menunggu_validasi')<form method="POST" action="{{ route('guru.records.validate',$record) }}" class="inline-actions">@csrf<button class="button primary" name="status_validasi" value="disetujui">Setujui</button><button class="button outline" name="status_validasi" value="ditolak">Tolak</button></form>@else - @endif</td></tr>@empty<tr><td colspan="5">Belum ada catatan.</td></tr>@endforelse</tbody></table></div>{{ $records->links() }}</section></x-layouts.app>
