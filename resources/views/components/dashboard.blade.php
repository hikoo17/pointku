@props(['title','eyebrow'=>'DASHBOARD','copy'=>'Pantau data dan tindak lanjut dalam satu ruang kerja.'])
<div class="page-intro"><div><p class="eyebrow">{{ $eyebrow }}</p><h1>{{ $title }}</h1><p class="muted">{{ $copy }}</p></div></div>
