name: "Checkout Repository"

on: 
  push:
    branches:
      - main

jobs:
  checkout-repo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: View Repository Files
        run: ls -la